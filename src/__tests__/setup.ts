/**
 * setup.ts
 *
 * Global test setup loaded once before any test files run.  Registers all
 * custom elements so unit tests can render them via document.createElement.
 */
import '../components/index.js';
