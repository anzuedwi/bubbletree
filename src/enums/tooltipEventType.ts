/**
 * tooltipEventType.ts
 *
 * Whether the tooltip should be shown (cursor entered a bubble)
 * or hidden (cursor left a bubble).
 */
export enum TooltipEventType {
  Show = 'SHOW',
  Hide = 'HIDE',
}
