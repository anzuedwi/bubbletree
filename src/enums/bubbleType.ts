/**
 * bubbleType.ts
 *
 * Identifies which bubble renderer is used at each level of the tree.
 * Pass one or more values via {@link BubbleConfig.bubbleType}.
 */
export enum BubbleType {
  Plain = 'plain',
  Donut = 'donut',
  Icon = 'icon',
}
